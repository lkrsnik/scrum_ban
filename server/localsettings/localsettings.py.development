import os

# Database
# https://docs.djangoproject.com/en/1.7/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'smart_housedb',
        'USER': 'regularuser',
        'PASSWORD': 'theregular',
        'HOST': 'localhost',
        'PORT': '3306',
    }
}